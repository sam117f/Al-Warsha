name: Al-Warsha Solid Build 🧱

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # هذه الخطوة ستحضر أداة البناء بنفسها ولن تعتمد على ملفاتك
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: '8.4'

      # هنا نأمر بالبناء المباشر
      - name: Build APK Directly
        run: gradle assembleDebug --stacktrace --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Al-Warsha-Final
          path: app/build/outputs/apk/debug/app-debug.apk
          
