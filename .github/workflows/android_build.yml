name: Al-Warsha Rescue Mission 🚑

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  rescue_build:
    runs-on: ubuntu-latest
    steps:
      # 1. استدعاء الكود
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2. تجهيز الجافا
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3. الحل الجذري: توليد ملف التشغيل الناقص
      - name: Fix Missing Gradle Wrapper
        run: gradle wrapper --gradle-version 8.4

      # 4. إعطاء صلاحيات التشغيل
      - name: Grant Permission
        run: chmod +x gradlew

      # 5. بناء التطبيق
      - name: Build APK
        run: ./gradlew assembleDebug

      # 6. إرسال التطبيق لك
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Al-Warsha-Final
          path: app/build/outputs/apk/debug/app-debug.apk
